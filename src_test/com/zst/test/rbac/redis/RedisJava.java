package com.zst.test.rbac.redis;

/**
 * 它通常被称为数据结构服务器，因为值（value）可以是 字符串(String), 
 * 哈希(Map), 列表(list), 集合(sets) 和 有序集合(sorted sets)等类型
 * @author zsjr
 *
 */
public class RedisJava {
	public static void main(String[] args) {
		System.out.println((int)(Math.random()*4));
	}
}
